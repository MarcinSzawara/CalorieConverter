export class Counter {
    constructor(result, text, gramsInput, allInputProduct, start, index) {
        this.result = result;
        this.text = text;
        this.gramsInput = gramsInput;
        this.allInputProduct = allInputProduct;
        this.index = index;
        start.addEventListener('click', this.checkInput.bind(this));
    }
    ;
    checkInput() { }
    ;
    mainFunction() { }
    ;
}
;
